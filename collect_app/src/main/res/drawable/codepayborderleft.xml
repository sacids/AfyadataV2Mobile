<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fond transparent -->
    <item android:drawable="@android:color/transparent" />
    <!-- Bordure rouge à gauche -->
    <item
        android:left="0dp"
        android:right="1dp"
        android:top="0dp"
        android:bottom="0dp">

        <shape>
            <solid android:color="@android:color/transparent" /> <!-- Fond transparent -->
            <stroke
                android:width="1dp"
                android:color="#a60904" /> <!-- Couleur rouge -->
        </shape>
    </item>
</layer-list>
